{
  "name": "object-storage-console",
  "version": "1.0.0",
  "description": "Object Storage Console - S3-compatible web console with Playwright E2E tests",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:auth": "playwright test auth.spec.ts",
    "test:dashboard": "playwright test dashboard.spec.ts",
    "test:buckets": "playwright test buckets.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
